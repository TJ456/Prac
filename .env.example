# 🔒 ENVIRONMENT VARIABLES FOR TASKFLOW PROJECT
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!

# 🚀 SERVER CONFIGURATION
PORT=5000
# Why: Defines which port your server runs on
# Default: 5000, but can be any available port

# 🗄️ DATABASE CONFIGURATION  
MONGO_URI=mongodb://localhost:27017/taskflow
# Why: Connection string to your MongoDB database
# Local MongoDB: mongodb://localhost:27017/taskflow
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/taskflow

# 🔐 JWT CONFIGURATION
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long
# Why: Secret key used to sign and verify JWT tokens
# CRITICAL: Use a strong, random secret (minimum 32 characters)
# Generate online or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 🌍 ENVIRONMENT
NODE_ENV=development
# Why: Helps your app behave differently in dev vs production
# Values: development, production, test

# 📧 EMAIL CONFIGURATION (for future features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASS=your_app_password

# 🎯 FRONTEND URL (for CORS)
# CLIENT_URL=http://localhost:3000
# Why: URL of your frontend app for CORS configuration
